{% set name = "apache-airflow-providers-ydb" %}
{% set version = "2.2.1" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/apache_airflow_providers_ydb-{{ version }}.tar.gz
  sha256: 2399e29ac3a843d14577c1719687f688722aadd5ce939f1db801e400e6f76756

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - flit-core ==3.12.0
    - pip
  run:
    - python >={{ python_min }}
    - apache-airflow >=2.10.0
    - apache-airflow-providers-common-sql >=1.20.0
    - ydb >=3.18.8
    - ydb-dbapi >=0.1.0

test:
  imports:
    - airflow.providers.ydb
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/apache/airflow/
  summary: Provider package apache-airflow-providers-ydb for Apache Airflow
  license: Apache-2.0
  license_file: src/airflow/providers/ydb/LICENSE
  license_family: Apache
  doc_url: https://airflow.apache.org/docs/apache-airflow-providers-ydb/stable/index.html
  dev_url: https://github.com/apache/airflow/

extra:
  recipe-maintainers:
    - xylar
